```bash
# 1. Ajoutez le dépôt PPA contenant les dernières versions de PHP
sudo add-apt-repository ppa:ondrej/php

# 2.Mettre à jour la liste des paquets
sudo apt update

# 3. Installer PHP 8.4
sudo apt install php8.4
```