{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
<div class="container-form p-6 border rounded-[0.5rem] border-[#D9D9D9] shadow-md">


			{{ form_errors(form) }}
			{{ form_start(form) }}

			<div class="flex flex-col mb-6">

				{{ form_label(form.supplement)}}
				{{ form_widget(form.supplement, {'attr': {'class': 'border rounded-[0.5rem] p-1'}})}}
				{{ form_errors(form.supplement)}}
			</div>
		
            <button type="submit" class="btn bg-[#2C2C2C] text-white border rounded-[0.5rem] w-full p-2">Ajouter</button>
			{{ form_end(form) }}
		</div>
