{{ form_start(form, {'attr': {'class': 'flex flex-col'}}) }}
{{ form_errors(form) }}

<div class="mb-3">
	{{ form_widget(form.content, {'attr': {'class': 'w-full p-2 border-0 focus:ring-0 focus:outline-none bg-transparent resize-none text-gray-800', 'rows': 5}}) }}
	{{ form_errors(form.content) }}
</div>

<div class="flex justify-end gap-2 mt-2">
	<a href="{{ path('app_notes_by_supplement', {'id': userSupplement.id}) }}" class="bg-[#6E6F6F] hover:bg-gray-400 text-white font-medium py-2 px-6 rounded-lg transition-colors">
		Annuler
	</a>
	<button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-6 rounded-lg transition-colors">
		Ajouter
	</button>
</div>
{{ form_end(form) }}
