{% extends 'base.html.twig' %}

{% block title %}Edit Note{% endblock %}

{% block body %}
    <section class="flex flex-col items-center p-6 min-h-screen w-screen gap-6 mb-12 ">
		{% for flash_error in app.flashes('verify_email_error') %}
			<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
		{% endfor %}

		<h1 class="text-[#308AFF] text-[2rem]">Edit Note</h1>


        {{ include('note/_form.html.twig', {'button_label': 'Modifier'}) }}

        {{ include('note/_delete_form.html.twig') }}

		<a href="{{ path('app_notes_by_supplement', { id: userSupplement.id }) }}">Annuler</a>

	</section>
{% endblock %}
